# 2026-05-13

## 修复 blogData is not defined（根本原因）

- **问题**：网站持续报错 `blogData is not defined`，之前的修复（调整脚本加载顺序）未能解决
- **根本原因**：`js/data.js` 中6处 Markdown 代码围栏（` ``` `）未转义，导致 JS 模板字符串提前终止，整个文件解析失败，`blogData` 变量从未被定义
- **修复**：将所有 ` ``` ` 替换为 `` \`\`\` ``（反引号转义）
- **影响行**：273、281（正念冥想文章）、467、469、473、475（自我关怀文章）
- **提交**：`d95e423 - 修复：转义 data.js 模板字符串中的反引号代码围栏（6处）`
- **已推送**：`65d7041..d95e423 main -> main`

## 教训

今后在 JS 模板字符串（backtick string）中写 Markdown 内容时，代码围栏 `` ``` `` 必须转义为 `` \`\`\` ``。
